<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodPlannerApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">#FFFFFF</item>

        <item name="android:colorButtonNormal">@color/darkGreen</item>
        <item name="android:statusBarColor">@color/darkGreen</item>
        <item name="bottomAppBarStyle">@style/BottomNavigationViewStyle</item>
        <item name="android:textColor">@color/darkGreen</item>
        <!-- <item name="android:navigationBarColor">#DBE7C9</item>-->
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
</resources>